{
	viewer {
		login
		followers {
			totalCount
		}
		repositories(first: 100) {
			totalCount

			nodes {
				name
				stargazerCount
				forkCount
				primaryLanguage {
					name
				}
			}
		}
	}
}
